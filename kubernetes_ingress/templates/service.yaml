apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes_ingress.fullname" . }}
  labels:
    {{- include "kubernetes_ingress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes_ingress.selectorLabels" . | nindent 4 }}
